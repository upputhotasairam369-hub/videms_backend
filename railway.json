{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && python core/manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "cd core && gunicorn core.wsgi:application --bind 0.0.0.0:$PORT"
  }
}
